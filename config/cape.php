<?php

// config/cape.php — CAPE (Context-Aware Prompt Engine) Configuration

return [

    /*
    |--------------------------------------------------------------------------
    | Speed Thresholds (km/h)
    |--------------------------------------------------------------------------
    */
    'speed' => [
        'low'    => 20,   // < 20 km/h = low speed
        'medium' => 60,   // 20–60 km/h = medium speed
        // > 60 km/h = high speed
    ],

    /*
    |--------------------------------------------------------------------------
    | Lux (Light) Thresholds
    |--------------------------------------------------------------------------
    */
    'lux' => [
        'very_low' => 50,   // < 50 = very low light (night)
        'low'      => 200,  // 50–200 = low light
        // > 200 = normal light
    ],

    /*
    |--------------------------------------------------------------------------
    | Rain Thresholds (%)
    |--------------------------------------------------------------------------
    */
    'rain' => [
        'none'  => 0,   // 0% = no rain
        'light' => 30,  // < 30% = light rain
        // >= 30% = heavy rain
    ],

    /*
    |--------------------------------------------------------------------------
    | Humidity Thresholds (%)
    |--------------------------------------------------------------------------
    */
    'humidity' => [
        'normal' => 70,  // < 70% = normal
        // >= 70% = high humidity
    ],

    /*
    |--------------------------------------------------------------------------
    | Flood Risk Levels
    |--------------------------------------------------------------------------
    */
    'flood_risk_levels' => ['Low', 'Medium', 'High', 'Critical'],

    /*
    |--------------------------------------------------------------------------
    | Proximity Danger Threshold (meters)
    |--------------------------------------------------------------------------
    */
    'proximity_danger_threshold' => 5.0,

    /*
    |--------------------------------------------------------------------------
    | Prompt Weights (higher = more critical)
    |--------------------------------------------------------------------------
    */
    'prompt_weights' => [
        'obstacle' => 5,
        'flood'    => 4,
        'light'    => 3,
        'speed'    => 2,
        'weather'  => 1,
    ],

    /*
    |--------------------------------------------------------------------------
    | LLM Configuration (Google Gemini via OpenAI-compatible endpoint)
    |--------------------------------------------------------------------------
    */
    'openai_model'    => 'gemini-2.0-flash',
    'openai_api_key'  => env('GEMINI_API_KEY'),
    'openai_base_url' => 'https://generativelanguage.googleapis.com/v1beta/openai',

    /*
    |--------------------------------------------------------------------------
    | Event Memory Cache Settings
    |--------------------------------------------------------------------------
    */
    'cache_key' => 'cape_event_memory',
    'cache_ttl' => 300, // 5 minutes in seconds

    /*
    |--------------------------------------------------------------------------
    | Frontend Poll Interval (milliseconds)
    |--------------------------------------------------------------------------
    */
    'poll_interval' => 20000, // 20 seconds

];
